<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta charset="UTF-8">
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1 class="title">Mini Message Board</h1>

        <div class="message-list">
            <% for (let i = 0; i < messages.length; i++) { %>
                <div class="message-card">
                    <p class="message-text"><%= messages[i].message %></p>
                    <p class="message-user">— <%= messages[i].name %></p>
                    <p class="message-date"><%= messages[i].date.toLocaleString() %></p>
                    
                </div>
            <% } %>
        </div>

        <div class="add-message">
            <a href="/new" class="add-button">➕ Add a new message</a>
        </div>
    </div>
</body>
</html>